// Generated by cpptb-codegen from stalling_responder.sv.
// Do not edit by hand.
#pragma once

#include <array>
#include <cstddef>
#include <cstdint>

#include "cpptb/dpi_static_binding.hpp"
#include "cpptb/hierarchy.hpp"

namespace cpptb::generated::stalling_responder {

enum SignalId : uint32_t {
    kSignalClk,
    kSignalRstN,
    kSignalRequest,
    kSignalStall,
    kSignalLatency,
    kSignalRequestData,
    kSignalResponseValid,
    kSignalResponseData,
    kSignalBusy,
    kCpptbSignalCount,
};

extern "C" {
}  // extern "C"

struct HierarchyTransport {
    template <std::size_t Width>
    static cpptb::probe::Value<Width> get(std::uint32_t id,
                                           std::int32_t index) {
        if constexpr (Width <= 32) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 32 && Width <= 64) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 64) {
            switch (id) {
                default: break;
            }
        }
        fail("get", id);
    }

    template <std::size_t Width>
    static void deposit(std::uint32_t id, std::int32_t index,
                        cpptb::probe::Value<Width> value) {
        if constexpr (Width <= 32) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 32 && Width <= 64) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 64) {
            switch (id) {
                default: break;
            }
        }
        fail("deposit", id);
    }

    template <std::size_t Width>
    static void force(std::uint32_t id, std::int32_t index,
                        cpptb::probe::Value<Width> value) {
        if constexpr (Width <= 32) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 32 && Width <= 64) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 64) {
            switch (id) {
                default: break;
            }
        }
        fail("force", id);
    }

    template <std::size_t Width>
    static cpptb::LogicBits<Width> get_logic(
        std::uint32_t id, std::int32_t index) {
        switch (id) {
            default: break;
        }
        fail("get_logic", id);
    }

    template <std::size_t Width>
    static void deposit_logic(
        std::uint32_t id, std::int32_t index,
        cpptb::LogicBits<Width> value) {
        switch (id) {
            default: break;
        }
        fail("deposit_logic", id);
    }

    template <std::size_t Width>
    static void force_logic(
        std::uint32_t id, std::int32_t index,
        cpptb::LogicBits<Width> value) {
        switch (id) {
            default: break;
        }
        fail("force_logic", id);
    }

    static void release(std::uint32_t id, std::int32_t index) {
        switch (id) {
            default: break;
        }
        fail("release", id);
    }

    static cpptb::coro::Signal signal(std::uint32_t id,
                                      const char* name) {
        switch (id) {
            default: break;
        }
        fail("edge", id);
    }

private:
    [[noreturn]] static void fail(const char* operation,
                                  std::uint32_t id) {
        std::fprintf(stderr,
                     "cpptb: hierarchy %s was not selected for signal %u\n",
                     operation, id);
        std::abort();
    }
};

struct Dut {
    static constexpr bool cpptb_static_binding = true;
    cpptb::dpi::StaticPackedSignal<1, true, false, kSignalClk, 0> clk;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalRstN, 0> rst_n;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalRequest, 1> request;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalStall, 2> stall;
    cpptb::dpi::StaticPackedSignal<8, true, true, kSignalLatency, 3> latency;
    cpptb::dpi::StaticPackedSignal<32, true, true, kSignalRequestData, 4> request_data;
    cpptb::dpi::StaticPackedSignal<1, false, false, kSignalResponseValid, 1> response_valid;
    cpptb::dpi::StaticPackedSignal<32, false, false, kSignalResponseData, 2> response_data;
    cpptb::dpi::StaticPackedSignal<1, false, false, kSignalBusy, 3> busy;
    [[no_unique_address]] cpptb::hierarchy::Signal<HierarchyTransport, 0, "countdown", 8, true, cpptb::probe::Value<8>, true> countdown;
    [[no_unique_address]] cpptb::hierarchy::Signal<HierarchyTransport, 1, "pending_data", 32, true, cpptb::probe::Value<32>, true> pending_data;
    [[no_unique_address]] cpptb::hierarchy::Signal<HierarchyTransport, 2, "stalled_request", 1, true, cpptb::probe::Value<1>, true> stalled_request;
};

using StallingResponderDut = Dut;

}  // namespace cpptb::generated::stalling_responder
