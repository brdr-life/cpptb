// Generated by cpptb-codegen from apb_regfile.sv.
// Do not edit by hand.
#pragma once

#include <array>
#include <cstddef>
#include <cstdint>

#include "cpptb/dpi_static_binding.hpp"
#include "cpptb/hierarchy.hpp"

namespace cpptb::generated::apb_regfile {

enum SignalId : uint32_t {
    kSignalClk,
    kSignalRstN,
    kSignalApbSelect,
    kSignalApbEnable,
    kSignalApbWrite,
    kSignalApbAddress,
    kSignalApbWriteData,
    kSignalApbReadData,
    kSignalApbReady,
    kSignalApbError,
    kCpptbSignalCount,
};

extern "C" {
}  // extern "C"

struct HierarchyTransport {
    template <std::size_t Width>
    static cpptb::probe::Value<Width> get(std::uint32_t id,
                                           std::int32_t index) {
        if constexpr (Width <= 32) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 32 && Width <= 64) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 64) {
            switch (id) {
                default: break;
            }
        }
        fail("get", id);
    }

    template <std::size_t Width>
    static void deposit(std::uint32_t id, std::int32_t index,
                        cpptb::probe::Value<Width> value) {
        if constexpr (Width <= 32) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 32 && Width <= 64) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 64) {
            switch (id) {
                default: break;
            }
        }
        fail("deposit", id);
    }

    template <std::size_t Width>
    static void force(std::uint32_t id, std::int32_t index,
                        cpptb::probe::Value<Width> value) {
        if constexpr (Width <= 32) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 32 && Width <= 64) {
            switch (id) {
                default: break;
            }
        }
        if constexpr (Width > 64) {
            switch (id) {
                default: break;
            }
        }
        fail("force", id);
    }

    template <std::size_t Width>
    static cpptb::LogicBits<Width> get_logic(
        std::uint32_t id, std::int32_t index) {
        switch (id) {
            default: break;
        }
        fail("get_logic", id);
    }

    template <std::size_t Width>
    static void deposit_logic(
        std::uint32_t id, std::int32_t index,
        cpptb::LogicBits<Width> value) {
        switch (id) {
            default: break;
        }
        fail("deposit_logic", id);
    }

    template <std::size_t Width>
    static void force_logic(
        std::uint32_t id, std::int32_t index,
        cpptb::LogicBits<Width> value) {
        switch (id) {
            default: break;
        }
        fail("force_logic", id);
    }

    static void release(std::uint32_t id, std::int32_t index) {
        switch (id) {
            default: break;
        }
        fail("release", id);
    }

    static cpptb::coro::Signal signal(std::uint32_t id,
                                      const char* name) {
        switch (id) {
            default: break;
        }
        fail("edge", id);
    }

private:
    [[noreturn]] static void fail(const char* operation,
                                  std::uint32_t id) {
        std::fprintf(stderr,
                     "cpptb: hierarchy %s was not selected for signal %u\n",
                     operation, id);
        std::abort();
    }
};

struct Dut {
    static constexpr bool cpptb_static_binding = true;
    cpptb::dpi::StaticPackedSignal<1, true, false, kSignalClk, 0> clk;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalRstN, 0> rst_n;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalApbSelect, 1> apb_select;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalApbEnable, 2> apb_enable;
    cpptb::dpi::StaticPackedSignal<1, true, true, kSignalApbWrite, 3> apb_write;
    cpptb::dpi::StaticPackedSignal<8, true, true, kSignalApbAddress, 4> apb_address;
    cpptb::dpi::StaticPackedSignal<32, true, true, kSignalApbWriteData, 5> apb_write_data;
    cpptb::dpi::StaticPackedSignal<32, false, false, kSignalApbReadData, 1> apb_read_data;
    cpptb::dpi::StaticPackedSignal<1, false, false, kSignalApbReady, 2> apb_ready;
    cpptb::dpi::StaticPackedSignal<1, false, false, kSignalApbError, 3> apb_error;
    static constexpr std::int64_t ID_VALUE = 1129336916;
    [[no_unique_address]] cpptb::hierarchy::Signal<HierarchyTransport, 0, "address_valid", 1, true, cpptb::probe::Value<1>, true> address_valid;
    [[no_unique_address]] cpptb::hierarchy::Memory<HierarchyTransport, 1, "registers", 32, 0, 3, true, cpptb::probe::Value<32>, true> registers;
};

using ApbRegfileDut = Dut;

}  // namespace cpptb::generated::apb_regfile
