module peripheral_suite_dut #(
    parameter int APB_ADDR_WIDTH = 12,
    parameter int TIMER_CNT = 2,
    parameter int SPI_BUFFER_DEPTH = 10,
    parameter int SPI_LOG_BUFFER_DEPTH = 4
) (
    input  logic HCLK,
    input  logic HRESETn,

    input  logic [APB_ADDR_WIDTH-1:0] timer_PADDR,
    input  logic [31:0] timer_PWDATA,
    input  logic timer_PWRITE,
    input  logic timer_PSEL,
    input  logic timer_PENABLE,
    output logic [31:0] timer_PRDATA,
    output logic timer_PREADY,
    output logic timer_PSLVERR,
    output logic [(TIMER_CNT * 2)-1:0] timer_irq,

    input  logic [APB_ADDR_WIDTH-1:0] spi_PADDR,
    input  logic [31:0] spi_PWDATA,
    input  logic spi_PWRITE,
    input  logic spi_PSEL,
    input  logic spi_PENABLE,
    output logic [31:0] spi_PRDATA,
    output logic spi_PREADY,
    output logic spi_PSLVERR,
    output logic [7:0] spi_clk_div,
    output logic spi_clk_div_valid,
    input  logic [31:0] spi_status,
    output logic [31:0] spi_addr,
    output logic [5:0] spi_addr_len,
    output logic [31:0] spi_cmd,
    output logic [5:0] spi_cmd_len,
    output logic [3:0] spi_csreg,
    output logic [15:0] spi_data_len,
    output logic [15:0] spi_dummy_rd,
    output logic [15:0] spi_dummy_wr,
    output logic [SPI_LOG_BUFFER_DEPTH:0] spi_int_th_tx,
    output logic [SPI_LOG_BUFFER_DEPTH:0] spi_int_th_rx,
    output logic [SPI_LOG_BUFFER_DEPTH:0] spi_int_cnt_tx,
    output logic [SPI_LOG_BUFFER_DEPTH:0] spi_int_cnt_rx,
    output logic spi_int_en,
    output logic spi_int_cnt_en,
    output logic spi_int_rd_sta,
    output logic spi_swrst,
    output logic spi_rd,
    output logic spi_wr,
    output logic spi_qrd,
    output logic spi_qwr,
    output logic [31:0] spi_data_tx,
    output logic spi_data_tx_valid,
    input  logic spi_data_tx_ready,
    input  logic [31:0] spi_data_rx,
    input  logic spi_data_rx_valid,
    output logic spi_data_rx_ready,

    input  logic [APB_ADDR_WIDTH-1:0] i2c_PADDR,
    input  logic [31:0] i2c_PWDATA,
    input  logic i2c_PWRITE,
    input  logic i2c_PSEL,
    input  logic i2c_PENABLE,
    output logic [31:0] i2c_PRDATA,
    output logic i2c_PREADY,
    output logic i2c_PSLVERR,
    output logic i2c_interrupt,
    input  logic i2c_scl_pad_i,
    output logic i2c_scl_pad_o,
    output logic i2c_scl_padoen_o,
    input  logic i2c_sda_pad_i,
    output logic i2c_sda_pad_o,
    output logic i2c_sda_padoen_o
);
  apb_timer_peakrdl #(
      .APB_ADDR_WIDTH(APB_ADDR_WIDTH),
      .TIMER_CNT(TIMER_CNT)
  ) i_timer (
      .HCLK(HCLK),
      .HRESETn(HRESETn),
      .PADDR(timer_PADDR),
      .PWDATA(timer_PWDATA),
      .PWRITE(timer_PWRITE),
      .PSEL(timer_PSEL),
      .PENABLE(timer_PENABLE),
      .PRDATA(timer_PRDATA),
      .PREADY(timer_PREADY),
      .PSLVERR(timer_PSLVERR),
      .irq_o(timer_irq)
  );

  spi_master_apb_if_peakrdl #(
      .BUFFER_DEPTH(SPI_BUFFER_DEPTH),
      .APB_ADDR_WIDTH(APB_ADDR_WIDTH),
      .LOG_BUFFER_DEPTH(SPI_LOG_BUFFER_DEPTH)
  ) i_spi (
      .HCLK(HCLK),
      .HRESETn(HRESETn),
      .PADDR(spi_PADDR),
      .PWDATA(spi_PWDATA),
      .PWRITE(spi_PWRITE),
      .PSEL(spi_PSEL),
      .PENABLE(spi_PENABLE),
      .PRDATA(spi_PRDATA),
      .PREADY(spi_PREADY),
      .PSLVERR(spi_PSLVERR),
      .spi_clk_div(spi_clk_div),
      .spi_clk_div_valid(spi_clk_div_valid),
      .spi_status(spi_status),
      .spi_addr(spi_addr),
      .spi_addr_len(spi_addr_len),
      .spi_cmd(spi_cmd),
      .spi_cmd_len(spi_cmd_len),
      .spi_csreg(spi_csreg),
      .spi_data_len(spi_data_len),
      .spi_dummy_rd(spi_dummy_rd),
      .spi_dummy_wr(spi_dummy_wr),
      .spi_int_th_tx(spi_int_th_tx),
      .spi_int_th_rx(spi_int_th_rx),
      .spi_int_cnt_tx(spi_int_cnt_tx),
      .spi_int_cnt_rx(spi_int_cnt_rx),
      .spi_int_en(spi_int_en),
      .spi_int_cnt_en(spi_int_cnt_en),
      .spi_int_rd_sta(spi_int_rd_sta),
      .spi_swrst(spi_swrst),
      .spi_rd(spi_rd),
      .spi_wr(spi_wr),
      .spi_qrd(spi_qrd),
      .spi_qwr(spi_qwr),
      .spi_data_tx(spi_data_tx),
      .spi_data_tx_valid(spi_data_tx_valid),
      .spi_data_tx_ready(spi_data_tx_ready),
      .spi_data_rx(spi_data_rx),
      .spi_data_rx_valid(spi_data_rx_valid),
      .spi_data_rx_ready(spi_data_rx_ready)
  );

  apb_i2c_peakrdl #(
      .APB_ADDR_WIDTH(APB_ADDR_WIDTH)
  ) i_i2c (
      .HCLK(HCLK),
      .HRESETn(HRESETn),
      .PADDR(i2c_PADDR),
      .PWDATA(i2c_PWDATA),
      .PWRITE(i2c_PWRITE),
      .PSEL(i2c_PSEL),
      .PENABLE(i2c_PENABLE),
      .PRDATA(i2c_PRDATA),
      .PREADY(i2c_PREADY),
      .PSLVERR(i2c_PSLVERR),
      .interrupt_o(i2c_interrupt),
      .scl_pad_i(i2c_scl_pad_i),
      .scl_pad_o(i2c_scl_pad_o),
      .scl_padoen_o(i2c_scl_padoen_o),
      .sda_pad_i(i2c_sda_pad_i),
      .sda_pad_o(i2c_sda_pad_o),
      .sda_padoen_o(i2c_sda_padoen_o)
  );
endmodule
